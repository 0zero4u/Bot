# .env.example
# Final example configuration for the Delta Trading Bot (v6.0.8)
# Copy this file to .env and fill in your specific values.

# --- GENERAL BOT SETTINGS ---
LOG_LEVEL="info"
INTERNAL_WS_PORT=8082 # Port for the internal WebSocket between the listener and trader.
RECONNECT_INTERVAL=5000 # Milliseconds to wait before reconnecting to a closed Delta WebSocket.

# --- BYBIT LISTENER CONFIGURATION (for the separate price-signal process) ---
# The listener watches this symbol on Bybit for price signals.
# The base currency (e.g., BTC) should generally match the Delta product.
BYBIT_SYMBOL="BTCUSDT"
BYBIT_STREAM_URL="wss://stream.bybit.com/v5/public/spot"


# --- DELTA EXCHANGE - ACCOUNT & CONNECTION ---
# CRITICAL: Fill these with your actual API credentials from Delta Exchange.
# Ensure keys are generated for the same environment specified in the URLs below.
DELTA_API_KEY=""
DELTA_API_SECRET=""

# Set the URLs for the Delta Exchange environment you are using (e.g., Production-India).
# These MUST match the region your account was created in.
DELTA_BASE_URL="https://api.india.delta.exchange"
DELTA_WEBSOCKET_URL="wss://socket.india.delta.exchange"


# --- DELTA EXCHANGE - TRADING PARAMETERS ---
# The symbol and ID for the product you want to trade on Delta Exchange.
# These MUST match each other and exist in your chosen environment.
DELTA_PRODUCT_SYMBOL="BTCUSD"
DELTA_PRODUCT_ID="27"

# The leverage to use for the product. This must be a valid option for the chosen symbol.
DELTA_LEVERAGE="25"

# The number of contracts to trade per order.
ORDER_SIZE="1"


# --- DELTA EXCHANGE - STRATEGY & RISK MANAGEMENT ---
# The price difference from the last trade needed to trigger a new trade signal.
PRICE_THRESHOLD="20.00"

# The order placement strategy. This L1-optimized bot only supports 'limit_bbo'.
# Do not change this unless you modify the code to use the l2_orderbook stream.
ORDER_PLACEMENT_STRATEGY="limit_bbo"

# CRITICAL: Must be "true" for the bot's bracket order logic to work correctly.
USE_BRACKET_ORDERS="true"

# Price offsets for the bracket order's Take Profit and Stop Loss legs.
TAKE_PROFIT_OFFSET="100.0"
STOP_LOSS_OFFSET="50.0"

# Cooldown period in seconds after a position is closed before the bot will look for new trades.
COOLDOWN_SECONDS="30"

# How long the exchange should wait before cancelling open orders if the bot disconnects (in milliseconds).
CANCEL_ON_DISCONNECT_TIMEOUT_MS="60000"
