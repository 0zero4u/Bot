{
  "name": "bybit-delta-trading-bot",
  "version": "5.0.0",
  "description": "A state-aware trading bot that uses Bybit signals to execute single, controlled trades on Delta Exchange with a cooldown period.",
  "main": "trader.js",
  "scripts": {
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js",
    "delete": "pm2 delete ecosystem.config.js",
    "restart": "pm2 restart ecosystem.config.js",
    "logs": "pm2 logs",
    "monitor": "pm2 monit",
    "test": "node --test",
    "dev": "nodemon trader.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "pm2": "^5.4.1",
    "uuid": "^9.0.1",
    "ws": "^8.18.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
